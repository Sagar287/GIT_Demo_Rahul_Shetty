
public class _1_72_GIT_Version_Control {

	// rahul shetty video ( SECTION 35 ) - 328, 329, 330
	// GIT version control
	
	// 331
	// cmd commands -> 'git config --global user.name "rahul"'
	// cmd commands -> 'git config --global user.email "gsagar287@gmail.com"'
	
	// 332
	// cmd command ->  cd GIT_Stuff
	// cmd command ->  git init, by default dile initiated by this is hidden
	// we first need to add our code to stash -> commit -> GITHUB
	// total 100 files, only 80 files are commited, then only 80 are uploaded to GITHUB
	// stash is like first level of commit.
	// if there is no commited files, then no files are uploaded
	// 
}
